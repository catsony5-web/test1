# 보안 범위와 개발 규칙

웹 릴리스는 정적 웹앱만 포함합니다. 로컬 작업공간에 별도로 존재할 수 있는 모바일 개발본과 인증 테스트 서버는 웹 릴리스의 커밋·게시 대상이 아니며 저장·인증 경계도 다릅니다. 자동 검사를 통과했다는 사실을 실제 금융기관 연결이나 기기 보안 검증의 완료로 해석하지 않습니다.

## 데이터 경계

| 종류 | 보관·공개 범위 |
| --- | --- |
| 웹 거래·분류·설정 | 사용자 origin의 IndexedDB/localStorage. 앱 자체 암호화 없음 |
| JSON/XLSX 백업 | 사용자가 저장하는 평문 파일. Git·공개 서버에 두지 않음 |
| 공개 공모주 일정 | `data/ipo-calendar.json`만 배포. 개인 청약·배정·매도 기록은 포함하지 않음 |
| 선택적 네이티브 개발본의 기록 | 별도 암호화 저장소·잠금 어댑터. 해당 개발본이 있는 경우 로컬 `mobile/SECURITY.md` 확인 |
| 선택적 로컬 인증 개발본의 키·토큰 | 별도 `server/`의 세션 메모리. 웹앱의 금융 조회·운영 서버와 미연결 |

XSS가 같은 origin에서 실행되면 웹 저장소에 접근할 수 있습니다. CSP와 출력 정화는 이를 줄이는 수단이며, 브라우저 확장·로컬 악성 프로그램·기기 관리자까지 보호하는 암호화 경계가 아닙니다.

웹 저장은 기존 키를 유지하며 요청 순서와 묶음 저장을 처리합니다. 엑셀 가져오기·달력 변경은 저장 성공 후에 원본 상태를 바꾸고, 메모 저장 실패는 입력을 유지한 채 재시도할 수 있습니다. 이 동작은 저장 실패·화면 오표시를 줄이는 장치이며 웹 저장소에 암호화를 추가하는 것은 아닙니다.

## 입력과 자산

- 엑셀·복원 파일·공개 일정·거래 메모를 모두 외부 입력으로 취급합니다. 날짜·금액·필드 범위를 정규화하고 HTML 출력은 기존 이스케이프·메모 허용 목록을 사용합니다.
- CSP는 외부 스크립트, inline script, 객체·iframe, form 제출을 제한합니다. 현재 차트·메모의 동적 스타일 때문에 `style-src 'unsafe-inline'`은 필요하며 script에는 허용하지 않습니다.
- SheetJS와 아이콘은 `assets/`의 로컬 파일을 사용합니다. 업데이트 시 공식 출처·버전·라이선스를 확인하고 엑셀 읽기/쓰기 회귀 검사를 실행합니다. 루트 package.json에 없는 vendored 파일은 `npm audit`만으로 검사되지 않습니다.
- 서비스워커는 같은 origin의 공개 `APP_FILES`만 대상으로 하며 인증 헤더가 있는 요청, API, 외부 origin을 제외합니다. 응답의 `private/no-store`는 런타임 캐시에서 제외하고 조회는 현재 앱 캐시로 제한합니다.
- GitHub Pages의 meta CSP로는 `frame-ancestors` 같은 응답 헤더 전용 정책을 설정할 수 없습니다. 다른 호스팅을 사용할 때는 서버 응답 헤더로 별도 적용합니다.

## 개발·배포 파일

`npm run dev`는 `127.0.0.1`에만 바인딩하고 공개 허용 파일만 GET/HEAD로 제공합니다. 임의 Host, 경로 탈출, 심볼릭 링크·junction, 개인·개발 파일 요청은 거부합니다. 저장소 전체를 대상으로 범용 파일 서버를 실행하거나 터널로 노출하지 마세요.

`npm run build`는 같은 허용 목록만 `dist/`에 복사합니다. 루트 `.gitignore`는 개인 엑셀·백업, `.env*`, 인증서·서명키, 로컬 QA·시안 이미지를 제외합니다. 예외는 프로젝트 manifest와 공개 일정처럼 이름·용도가 정해진 파일만 둡니다. `.env.example`에는 실제 키를 넣지 않습니다.

`.gitignore`는 이미 추적 중인 파일을 제거하거나 원격 배포물을 바꾸지 않습니다. 커밋 전 diff를 검토하고 배포할 산출물만 업로드합니다. GitHub Pages는 GitHub Actions를 게시 소스로 사용하며 [deploy-pages.yml](.github/workflows/deploy-pages.yml)이 검증 → 공개 `dist/` 빌드 → 산출물 업로드 → 게시를 수행합니다. 저장소 루트를 그대로 게시하지 않습니다.

## CI·인증키

- PR 검증 workflow는 `contents: read`, checkout 인증정보 보존 해제, 비밀키 없는 테스트를 사용합니다.
- 공개 일정 수집 job은 일정 커밋·push를 위해 `contents: write`와 checkout 인증을 사용합니다. 수집 job 자체에는 Pages 게시 권한을 주지 않습니다.
- 웹 배포의 build job은 `contents: read`로 검증·빌드하고, deploy job만 `pages: write`, `id-token: write`를 사용합니다. 공개 일정이 변경되면 수집 workflow가 같은 배포 workflow를 재사용 호출합니다. 이 호출에 수집용 secret은 전달하지 않습니다.
- GitHub Actions는 검토한 commit SHA에 고정합니다. 버전 변경은 공식 release와 변경 내용을 확인하고 갱신합니다.
- `DART_API_KEY`는 기존 Actions secret을 수집 단계에만 제공합니다. 값이 들어간 URL·응답·환경 전체를 로그로 출력하지 않습니다.
- 실제 OAuth 인증, 금융 권한·계약, 배포 서명키 설정은 이번 자동 검사의 범위가 아닙니다.

## 문제 발견 시

공개 이슈에 실제 백업·거래·키를 첨부하지 말고 합성 입력으로 재현합니다. 비밀값 유출이 의심되면 소유자가 해당 키를 우선 폐기·재발급하고, 현재 파일 삭제뿐 아니라 Git 이력·배포물·로그의 노출 범위를 확인합니다. 이력 재작성이나 사용자 기록 삭제는 담당자와 영향 범위를 확인한 뒤 진행합니다.

검증 명령은 [CONTRIBUTING.md](CONTRIBUTING.md), 이번 검사 결과는 [유지보수 점검 기록](docs/maintenance-audit.md)에 있습니다.
