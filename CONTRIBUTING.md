# 개발·협업 가이드

이 저장소의 릴리스 대상은 정적 웹 가계부입니다. 로컬 작업공간에는 별도 작업의 모바일·인증 서버 개발본이 있을 수 있지만, 웹 릴리스에서는 커밋·게시하지 않습니다. 수정 위치를 찾을 때는 [코드 구조와 데이터 흐름](docs/architecture.md)을 먼저 확인하세요.

## 시작하기

1. 작업 전 `git status --short`와 관련 파일의 diff를 확인합니다. 이미 수정된 파일은 다른 작업자의 변경일 수 있습니다.
2. 새 작업은 `codex/작업명`과 같은 작업 브랜치에서 진행합니다. 다른 개발자는 팀의 브랜치 규칙을 따르며, 동시에 같은 파일을 다루면 담당 범위를 먼저 나눕니다.
3. 목적, 바뀌는 동작, 성공 조건, 확인 방법을 짧게 기록합니다. 불확실한 계산 규칙·데이터 이전·삭제 범위는 결정 전에 확인합니다.
4. 필요한 파일만 수정합니다. 요청과 관계없는 정리, 일괄 포맷 변경, 함수·파일 이동은 섞지 않습니다.

기존 변경을 덮어쓰거나 승인 없이 `git reset --hard`, `git checkout --`, stash 삭제를 실행하지 않습니다. 충돌을 피하기 어렵다면 별도 worktree를 사용하거나 담당자와 변경 범위를 조율합니다. 커밋 전에는 작업한 파일만 선택적으로 스테이징하고 diff를 다시 읽습니다.

## 개발 명령

루트 `package.json`의 scripts가 명령의 기준입니다. Node 버전은 해당 파일의 `engines`를 따르세요. 선택적 로컬 모바일·서버 개발본의 의존성은 각 프로젝트에서 별도로 관리합니다.

| 명령 | 목적 |
| --- | --- |
| `npm run dev` | `http://127.0.0.1:4173`에서 허용된 공개 웹 파일만 제공 |
| `npm test` | 루트 `tests/` 자동 검사 |
| `npm run check` | JavaScript 구문, 공개 자산 연결, CSP·인라인 스크립트 검사 |
| `npm run cache:sync` | HTML의 정확한 자산 URL과 버전 쿼리를 서비스워커 목록에 동기화 |
| `npm run test:qa` | `qa/verify-*.cjs` 회귀 검사 |
| `npm run verify` | `check`, `test`, `test:qa` 통합 실행 |
| `npm run build` | 공개 파일만 `dist/`에 복사하여 배포 산출물 생성 |
| `npm run preview` | `127.0.0.1:4174`에서 빌드된 `dist/`와 실제 서비스워커 확인 |

개발 서버의 루트로 저장소 전체를 그대로 공개하지 마세요. `.gitignore`는 HTTP 접근을 막지 않으므로 개인 파일이 Git에서 제외되어 있어도 잘못된 정적 서버에서는 노출될 수 있습니다.

`mobile/` 또는 `server/` 개발본을 별도로 보유한 경우에만 `npm --prefix mobile test`, `npm --prefix server test`를 실행합니다. 설치·동기화·실행은 해당 로컬 폴더의 README를 따릅니다. 웹 저장소를 새로 clone한 개발자에게 이 폴더들의 존재나 설치를 요구하지 않습니다.

## 수정 위치와 호환성

- JavaScript는 classic script의 공유 전역 구조입니다. 새 파일·함수를 추가할 때 이름 충돌과 `index.html`의 로드 순서를 확인합니다.
- CSS 숫자는 적용 순서입니다. 색 변경은 기존 토큰을 우선 사용하고, 화면 전용 규칙은 해당 스타일 파일에 둡니다. CSS 정리만으로 파일 번호나 로드 순서를 바꾸지 않습니다.
- 상태는 `app/state.js`, 저장은 `utils/storage.js`, 소비지출 집계는 `utils/grouping.js`와 분석 코어의 기존 경로를 재사용합니다.
- 새로운 입력은 날짜·금액·범위를 정규화하고 출력 전에 이스케이프합니다. 외부 파일·공개 API 응답도 신뢰하지 않는 입력입니다.
- 성능 변경은 느린 동작을 재현한 뒤 필요한 계산·렌더링만 줄입니다. 필터 조작에 불필요한 전체 재분류를 추가하거나 근거 없이 전체 구조를 다시 만들지 않습니다.

## 데이터와 테스트

자동 테스트에는 코드 안에서 생성한 합성 거래·가짜 기관 응답을 사용합니다. 실제 가맹점 이력, 계좌번호, 이름, 개인 백업이나 인증값을 테스트·스크린샷·콘솔 로그에 복사하지 않습니다. 루트 `tests/`에는 Node 내장 테스트와 classic script를 VM 문맥에서 불러오는 패턴이 있습니다. 계산 코어는 `tests/helpers/board-overview-context.cjs` 같은 기존 도우미를 참고하되 필요한 의존성만 불러오세요.

수동 테스트는 별도 브라우저 프로필이나 격리된 저장소에서 진행합니다. 사용자가 실제로 쓰는 프로필의 데이터 초기화를 검증 편의상 실행하지 않습니다. 테스트 파일과 내려받은 엑셀·JSON 백업은 저장소 밖에 보관합니다. 자세한 범위는 [수동 데이터 정책](qa/manual-test-data.md), [스모크 검사](qa/smoke-checklist.md), [회귀 체크리스트](qa/regression-checklist.md)를 참고하세요.

변경에 맞는 검증을 선택합니다.

| 변경 | 최소 확인 내용 |
| --- | --- |
| 계산·분류·기간 비교 | 합성 입력의 정확한 금액, 취소·수입·저축·대출·정산 처리, 데이터 없는 기간 |
| 저장·복원 | 기존 키와 이전 형식, 저장 실패, 중복 복원, 일부 범위 복원, 마지막 정상본·스냅샷 복구 |
| 날짜·달력 | 월 경계·연도 경계·현재 월 초기값, 날짜 선택, 중복 집계 여부 |
| 테마·레이아웃 | 해당 화면·테마, 모바일 너비, 텍스트 대비, 키보드 포커스, 가로 넘침, 콘솔 오류 |
| 공모주 수집 | 합성 응답, 정정·충돌·누락, 출처 실패 시 기존 파일 보존, 개인 기록 보존 |
| 공개 자산·캐시 | 경로 존재, 허용 목록, 서비스워커 갱신, 새 탭·기존 캐시·오프라인 진입 |
| 네이티브·서버 경계 | 하위 프로젝트 테스트, 실제 OS·기관 검증이 필요한 범위 명시 |

작은 표시 수정에 구현 내용을 그대로 복사한 테스트를 늘리지 않습니다. 계산·저장·보안 동작은 실패 조건을 재현하는 검사를 우선하며, 합성 검사가 실제 금융기관 연결이나 기기 암호화 검증을 대신한다고 기록하지 않습니다.

## 저장 키와 마이그레이션

`src/data/constants.js`의 `STORAGE_KEYS`, `LEGACY_STORAGE_KEYS`, `DB_NAME`, `DB_VERSION`은 기존 사용자 데이터와의 계약입니다. 테마·폴더·앱 버전을 정리하는 과정에서 저장 키까지 변경하지 않습니다.

형식 변경이 필요하면 이전 형식을 읽는 정규화, 마이그레이션 조건, 변경 전 스냅샷, 저장 실패 시 복구를 함께 검토합니다. 여러 범위를 함께 바꾸는 저장은 기존 `safeSaveMany()`와 백업의 범위별 저장 흐름을 확인하세요. 저장 어댑터를 우회한 `localStorage` 직접 쓰기는 모바일의 저장·잠금 경계를 깨뜨릴 수 있습니다.

`safeSaveMany()`는 요청 데이터를 복사하고 호출 순서대로 저장합니다. 거래와 관련 정보를 함께 바꾸는 작업은 한 묶음으로 저장하고 성공 후에만 원본 상태와 완료 표시를 갱신합니다. 메모의 지연 저장은 이전 응답이 새 입력의 저장 완료로 표시되지 않도록 변경 세대를 확인합니다. 저장 상태 표시와 스냅샷 목록 갱신은 분리되어 있으므로 매 저장마다 전체 목록을 다시 읽는 경로를 추가하지 마세요.

## 공개 파일·서비스워커·버전

화면에 새 자산을 연결하거나 경로를 옮기면 다음 연결을 함께 확인합니다.

1. `index.html`의 참조 경로와 classic script/CSS 로드 순서.
2. 개발 서버·웹 빌드의 공개 허용 목록과 실제 `dist/` 산출물.
3. `npm run cache:sync`로 `service-worker.js`의 `APP_FILES`를 HTML의 정확한 버전 URL과 맞춥니다. 개인 파일·인증 API를 추가하지 않습니다. 참조에서 제거한 자산은 목록에서도 직접 검토하여 정리합니다.
4. `src/data/constants.js`의 `APP_VERSION`과 서비스워커 `CACHE_NAME`의 릴리스 버전.
5. 바뀐 CSS·JavaScript의 `index.html` URL 쿼리. 캐시된 이전 파일과 섞이지 않도록 갱신합니다.
6. 해당 경로·버전에 의존하는 QA. 별도 모바일 개발본을 함께 작업하는 경우 그 자산 복사 경로도 확인합니다.

개별 파일의 `?v=`는 마지막으로 바뀐 버전을 유지할 수 있으므로 모든 URL을 같은 숫자로 일괄 수정할 필요는 없습니다. 그러나 이번에 바뀐 파일의 쿼리와 현재 앱·서비스워커 릴리스는 일관되어야 합니다. 주소창의 `?v=...`만 바꿔서 모든 하위 자산이 갱신된다고 가정하지 마세요.

공개 일정 `data/ipo-calendar.json`은 일반 개인 JSON의 예외입니다. 공개 출처와 데이터 형식을 확인한 뒤 정해진 수집 경로로 갱신합니다. 인증키는 실행 환경의 secret으로 전달하고 파일·URL·로그에 넣지 않습니다. 수집·정정 규칙은 [공모주 출처 가이드](docs/ipo-sources.md)에 있습니다.

## 리뷰와 배포 확인

리뷰에는 해결한 문제, 사용자가 보는 동작, 변경 파일의 역할, 수행한 검사와 남은 검증만 간단히 적습니다. 개인 데이터가 없는 합성 예시로 재현 방법을 제공합니다. 코드 변경과 실제 운영 배포는 구분하여 보고합니다.

GitHub Pages는 GitHub Actions를 게시 소스로 사용합니다. [deploy-pages.yml](.github/workflows/deploy-pages.yml)은 `main` 변경 시 `npm run verify` → `npm run build` → `dist/` 업로드 → Pages 게시 순서로 실행합니다. 검증 실패 시 게시하지 않습니다. PR에서는 [verify.yml](.github/workflows/verify.yml)이 검사와 공개 빌드만 수행합니다.

공개 일정 수집 workflow는 변경된 공개 데이터를 커밋한 뒤 동일한 배포 workflow를 재사용 호출합니다. 봇 push가 다음 workflow를 자동 실행할 것이라고 가정하지 않습니다. 수집 job과 배포 job의 성공을 각각 확인하세요.

배포를 요청받으면 다음을 확인합니다.

- `git diff --check`, 변경 관련 검사, `npm run verify`, `npm run build`가 통과한다.
- 스테이징 diff와 `dist/`에 개인 금융 파일, `.env`, 인증키, 로컬 서버·테스트·스크린샷이 포함되지 않는다.
- 배포 대상은 검증한 공개 산출물이며 저장소 전체 업로드가 아니다.
- 사이트가 프로젝트 하위 경로에서도 열리고 CSS·JavaScript·폰트·아이콘·공개 일정이 정상 로드된다.
- 새 브라우저 저장소에서 빈 데이터로 시작하고, 기존 사용자 환경에서는 기록과 테마가 유지된다.
- 새 서비스워커가 활성화되며 오래된 캐시만 정리한다. 첫 진입·새로고침·오프라인 재진입에서 오류가 없다.
- 배포 작업의 성공과 실제 공개 URL의 반영을 각각 확인한다. 공개 일정 수집 성공을 웹앱 전체 배포 성공으로 대신하지 않는다.

의존성 변경은 버전과 lockfile을 함께 검토하고 출처·라이선스를 확인합니다. 모바일과 서버는 자신의 manifest와 검증 절차를 사용하며, 루트 웹의 검증 통과를 두 프로젝트의 배포 승인으로 해석하지 않습니다.
